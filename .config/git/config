[user]
	name = Daniel Martí
	email = mvdan@mvdan.cc
	signingKey = ~/.ssh/sign_ed25519
[core]
	editor = helix
	pager = delta --hyperlinks # TODO: also hyperlinks for gerrit repos
	askPass = /usr/lib/seahorse/ssh-askpass
[interactive]
	diffFilter = delta --color-only
[include]
	path = ~/.config/git/themes.gitconfig
[delta]
	navigate = true # use n and N to move between diff sections
	file-style = omit
	hunk-header-style = file line-number syntax
	features = hoopoe zebra-light # for diff.colorMoved
[push]
	default = current
	autoSetupRemote = true
	followTags = iso
[checkout]
	defaultRemote = origin
[commit]
	verbose = true
	gpgSign = true
[rebase]
	autoStash = true
	autoSquash = true
	updateRefs = true
	missingCommitsCheck = error
[format]
	# signoff = true
[log]
	date = iso
[diff]
	indentHeuristic = true
	# colorMoved = default # it breaks under delta?
	# noprefix = true # it affects git-send-email!
[color.diff]
	oldMoved = blue
	newMoved = blue
	oldMovedAlternative = blue
	newMovedAlternative = blue
[merge]
	conflictStyle = diff3
[sendemail]
	smtpServer = /usr/bin/msmtp
	confirm = auto
[pull]
	rebase = merges
[fetch]
	parallel = 8
	prune = true
	pruneTags = true
[advice]
	skippedCherryPicks = false
[gpg]
	format = ssh
[init]
	defaultBranch = main
[credential]
	username = mvdan
	helper = /usr/lib/git-core/git-credential-libsecret
[gpg.ssh]
	allowedSignersFile = ~/.config/git/allowed_signers
[rerere]
	enabled = true

# Sort branches and tags by descending date first, then by descending name.
# This helps show the latest or highest ones first.
[branch]
	sort = -refname
	sort = -creatordate
[tag]
	sort = -refname
	sort = -creatordate
